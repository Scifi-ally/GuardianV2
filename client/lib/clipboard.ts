/**
 * Clipboard functionality removed - internal operations only
 * Returns false to indicate clipboard operations are disabled
 */
export const copyToClipboard = async (text: string): Promise<boolean> => {
  // Clipboard functionality removed for internal-only operation
  console.log("Clipboard operation disabled - internal operation only");
  return false;
};
